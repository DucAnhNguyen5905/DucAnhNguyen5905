#include<iostream>
using namespace std;

/*
// Bai 13
void tim_so_lon_nhat(double &n);

int main(){
    double so_lon_nhat;

    tim_so_lon_nhat(so_lon_nhat);
    return 0;}

void tim_so_lon_nhat(double &so_lon_nhat){
    int so_phan_tu;
    double mang[100];
    cout << "So phan tu: " << endl;
    cin >> so_phan_tu;
    cout << "Gia tri cua phan tu: " ;
    for(int i = 0; i < so_phan_tu; i++){
        cin >> mang[i];
        so_lon_nhat = 0;
        if (so_lon_nhat < mang[i]){
            so_lon_nhat = mang[i];}}
        cout << "so lon nhat la: " << so_lon_nhat << endl;}
    */
    /*
// Bai 14
void doi_gia_tri(int &x, int &y) {
  int z = x;
  x = y;
  y = z;
}

int main() {
	int so_thu_nhat = 10;
	int so_thu_hai = 20;
	
	doi_gia_tri(so_thu_nhat, so_thu_hai);
	
	cout << "Sau khi doi: " << endl;
	cout << so_thu_nhat << so_thu_hai << endl;
	
  	return 0;
}
*/
/*
// Bai 15
void thay_doi_gia_tri(int *mang, int phan_tu_can_thay_doi, int gia_tri_moi) {
    mang[phan_tu_can_thay_doi] = gia_tri_moi;
}

int main() {
    int mang[5] = {1, 2, 3, 4, 5};
    
    thay_doi_gia_tri(mang, 1, 10);

    std::cout << "Sau khi thay doi: ";
    for (int i = 0; i < 5; ++i) {
        std::cout << mang[i] << " ";
    }
    std::cout << std::endl;

    return 0;
}
*/
